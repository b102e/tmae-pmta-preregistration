# Random Seed

random_seed = 20260506

This seed is committed before the first pilot session as a pre-registration commitment.
Commit hash of this file = timestamp proof for OSF preregistration.

Used in:
- All permutation tests (PERMANOVA, block bootstrap)
- Bootstrap CI for M6 thresholds
- Model inference (torch.manual_seed) if deterministic mode available
